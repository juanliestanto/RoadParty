<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.roadparty">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.RoadParty">
        <activity android:name=".Profile.EmailActivity"></activity>
        <activity android:name=".Profile.PasswordActivity" />
        <activity android:name=".Register.ResetActivity" />
        <activity android:name=".Register.DaftarActivity" />
        <activity android:name=".Home.VoucherActivity" />
        <activity android:name=".Transaksi.StatusActivity" />
        <activity android:name=".VoucherSayaActivity" />
        <activity android:name=".Transaksi.PesanActivity" />
        <activity android:name=".Home.CuciSeterikaActivity" />
        <activity android:name=".Home.SeterikaActivity" />
        <activity android:name=".Home.MencuciActivity" />
        <activity android:name=".Profile.ProfileActivity" />
        <activity android:name=".Register.MasukActivity" />
        <activity android:name=".Home.MainActivity" />
        <activity android:name=".Register.BerandaActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>